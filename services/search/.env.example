# Local dev defaults match docker-compose.yml; the service runs without a
# .env at all on a default local setup.
DATABASE_URL=postgres://nordhem:nordhem@localhost:5432/nordhem
ES_URL=http://localhost:9200
SEARCH_INDEX=products
PORT=3001
